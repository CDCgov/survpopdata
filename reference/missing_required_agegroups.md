# Check if all required age groups are present

Check if all required age groups are present

## Usage

``` r
missing_required_agegroups(present_agegroups)
```

## Arguments

- present_agegroups:

  `str` Character vector of age group codes.

## Value

`str` `NA` if all present, otherwise a string of missing age groups.
